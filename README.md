# Scraper

---

**Scraper** is a lightweight command-line web scraping tool written in Java.  
It allows you to fetch, parse, and extract data from web pages directly from the terminal.

## Features
- Fetch HTML content from any URL
- Extract links, text, and elements
- CLI-based interface
- Simple and extendable Java codebase
